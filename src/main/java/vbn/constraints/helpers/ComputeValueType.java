package vbn.constraints.helpers;

import vbn.constraints.Value.Value;

public class ComputeValueType {

//    private static final Map<>

    public static Value.Type getType(Object value) {
        return Value.Type.INT_TYPE;
    }
}

